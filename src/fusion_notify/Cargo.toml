[package]
name = "fusion_notify"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = "0.10"
ic-cdk = "0.18.0"
ic-cdk-timers = "0.12.0" # Feel free to remove this dependency if you don't need timers