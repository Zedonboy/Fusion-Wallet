type HttpRequest = record {
  url : text;
  method : text;
  body : blob;
  headers : vec record { text; text };
  certificate_version : opt nat16;
};
type HttpResponse = record {
  body : blob;
  headers : vec record { text; text };
  upgrade : opt bool;
  status_code : nat16;
};
type PaymentLink = record {
  id : text;
  token_symbol : text;
  memo : text;
  recipient : text;
  created_at : nat64;
  amount : text;
  qr_data : text;
};
type PaymentLinkRequest = record {
  memo : text;
  token_address : text;
  amount : text;
};
type Result = variant { Ok : text; Err : text };
service : () -> {
  create_payment_link : (PaymentLinkRequest) -> (Result);
  export_candid : () -> (text) query;
  get_payment_links : () -> (vec PaymentLink) query;
  http_request : (HttpRequest) -> (HttpResponse) query;
}
